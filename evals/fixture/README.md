# studio-site

Marketing site for a small design studio. `npm run dev` starts Astro on
`:4321`. The hero is a WebGL scene (`src/scene/scenes/home/HomeScene.ts`)
rendered into one persistent canvas; the rest is Astro pages.
